class Solution {
    public String toLowerCase(String s) {
        String newstr="";
        for(int i=0;i<s.length();i++){
            char ch=Character.toLowerCase(s.charAt(i));
        newstr+=ch;
        }
        
      return newstr;
    }
}
